//Вам дан класс Book (Книга) с двумя полями: String title (название) и int pageCount (количество страниц).
//Ваша задача — имплементировать интерфейс Comparable<Book> в классе Book и отсортировать книги по количеству
//страниц по возрастанию (от меньшего к большему).

//Создайте класс Student с полями String name и double score (балл). Имплементируйте Comparable<Student> и отсортируйте
//студентов по набранным баллам по убыванию (от высшего балла к низшему). Студент с самым высоким баллом должен быть
//первым в списке.

//Вам дан класс Car (String model, double price). Вносить изменения внутрь класса Car
//(то есть писать implements Comparable) запрещено!
//Создайте отдельный класс CarPriceComparator, который имплементирует Comparator<Car>.
//С помощью этого внешнего компаратора отсортируйте автомобили от дешевых к дорогим.

//Создайте класс Product (String name, double price). Необходимо отсортировать продукты по названию в алфавитном порядке.
//Но на этот раз вместо создания отдельного класса передайте в метод sort Lambda-выражение.


//Создайте класс Employee (String name, double salary).
//Отсортируйте сотрудников по зарплате по убыванию. Если у двоих сотрудников зарплата одинаковая,
//отсортируйте их между собой по имени в алфавитном порядке.





//Вам дан список (List), состоящий из произвольных чисел от 1 до 20. Ваша задача — с помощью Stream API найти из этого
// списка только четные числа, собрать их в новый список (List) и вывести на экран.

//List<Integer> numbers = Arrays.asList(5, 12, 8, 19, 24, 3, 10);



//У вас есть список имен, написанных строчными (маленькими) буквами (например: "Sada", "Şəhanə", "Ayşən","Aysel").
// Вы должны перевести все имена в этом списке в верхний регистр (ALL CAPS / UpperCase).
// Использовать классический цикл for ЗАПРЕЩЕНО.



//Вам дан список языков программирования: "Java", "Python", "JavaScript", "C++", "C#".
//Ваша задача — найти языки, названия которых начинаются на букву "J", и сразу вывести их на экран,
//не сохраняя в новый список (List).



//Эта задача состоит из двух частей:
//
//Отсортируйте данный список неупорядоченных чисел (5, 1, 9, 2, 7) по возрастанию с помощью sorted() и выведите его на экран.
//
//Найдите количество чисел в этом списке, которые больше 5, с помощью count().
