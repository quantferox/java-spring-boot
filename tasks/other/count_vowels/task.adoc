# Count Vowels

## Описание

Метод `countVowels(String value)` — подсчитать количество гласных букв (a, e, i, o, u)
в строке. Регистр не важен. Null-safe: при `null` вернуть `0`.

## Запуск

```java
Main.main(args)
// Пример: countVowels("Hello World") → 3
// Пример: countVowels(null) → 0
```
